---
phase: 03-tenant-scoped-data-source-selection
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - edge-functions/api/data/security-events.js
  - index.html
  - app.js
autonomous: false
requirements: [DATA-02, DATA-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A logged-in user can select Security Events and receive a response scoped exclusively to their own EdgeOne account, or a generic no-data state — never another tenant's data or a raw error."
    - "A client-supplied scoping parameter (e.g. a query-string zoneId/tenant_id) has zero effect on which tenant's data is returned, for both data sources."
    - "The selected data source (CDN Traffic Stats vs Security Events) is carried forward as short-lived request state, not persisted long-term server-side."
  artifacts:
    - path: "edge-functions/api/data/security-events.js"
      provides: "Session-gated Edge Function route calling DescribeDDoSAttackData, scoped server-side only — mirrors cdn-traffic.js's structure exactly"
    - path: "index.html"
      provides: "Two-card picker (CDN Traffic Stats, Security Events), both login-gated"
    - path: "app.js"
      provides: "Click handler for the Security Events card; selected-source state passed forward via a short-lived query param (D-04)"
  key_links:
    - from: "app.js"
      to: "edge-functions/api/data/security-events.js"
      via: "fetch('/api/data/security-events') on Security Events card click"
      pattern: "fetch\\('/api/data/security-events'\\)"
    - from: "edge-functions/api/data/security-events.js"
      to: "edge-functions/lib/tenant-mapping.js"
      via: "import { getTenantAccount } from '../../lib/tenant-mapping.js' — same tenant lookup reused from Plan 03-01, no duplicated logic"
      pattern: "getTenantAccount\\("
    - from: "edge-functions/api/data/security-events.js"
      to: "edge-functions/lib/teo-signer.js"
      via: "import { signTeoRequest } from '../../lib/teo-signer.js' — signs the real DescribeDDoSAttackData call"
      pattern: "signTeoRequest\\("
    - from: "edge-functions/api/data/security-events.js"
      to: "edge-functions/lib/session.js"
      via: "import { verifySession } from '../../lib/session.js' — verify session first, reject unauthenticated with 401 before any KV/API call"
      pattern: "verifySession\\("
---

<objective>
Expand the tracer proven in Plan 03-01 to the second data source — Security Events (DDoS attack data) — by replicating the exact same session-verify -> tenant-lookup -> signed-API-call pattern against `DescribeDDoSAttackData`, complete the two-card picker UI (D-01), wire short-lived data-source-selection state forward for Phase 4 (D-04), and run the full live verification covering all three ROADMAP Phase 3 success criteria including the DATA-03 negative test (client-supplied scoping input proven to have zero effect).

Purpose: Closes the loop the tracer (03-01) opened — proves the same tenant-scoping mechanism generalizes to a second, independently-versioned API action without any change to the security-critical path (`verifySession()` -> `getTenantAccount()` -> `signTeoRequest()`), and proves the negative case that no prior plan explicitly tested.

Output: `edge-functions/api/data/security-events.js`, a complete two-card picker in `index.html`/`app.js` with short-lived selection-state passthrough, and a completed live verification checkpoint covering DATA-01, DATA-02, DATA-03.
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tenant-scoped-data-source-selection/03-CONTEXT.md
@.planning/phases/03-tenant-scoped-data-source-selection/03-RESEARCH.md
@.planning/phases/03-tenant-scoped-data-source-selection/03-01-SUMMARY.md
@edge-functions/api/data/cdn-traffic.js
@edge-functions/lib/tenant-mapping.js
@edge-functions/lib/teo-signer.js
@index.html
@app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Security Events route + complete two-card picker with selection passthrough</name>
  <read_first>
    edge-functions/api/data/cdn-traffic.js
    edge-functions/lib/tenant-mapping.js
    edge-functions/lib/teo-signer.js
    .planning/phases/03-tenant-scoped-data-source-selection/03-RESEARCH.md (Standard Stack, Pitfall 1, Pitfall 4)
    index.html
    app.js
  </read_first>
  <files>edge-functions/api/data/security-events.js, index.html, app.js</files>
  <action>
    Create `edge-functions/api/data/security-events.js`, structurally identical to `edge-functions/api/data/cdn-traffic.js` (verify session first, reject unauthenticated with 401 before any KV/API call, resolve tenant via the unchanged `getTenantAccount(payload.tenant_id)`, never read scoping from client input, never forward `Response.Error` or credentials) — differing only in the `signTeoRequest` call: `action: 'DescribeDDoSAttackData'`, `version: '2022-09-01'` (per 03-RESEARCH.md Pitfall 1 — this version differs from `cdn-traffic.js`'s `2022-01-06`, do not share a constant across both routes), `payload: { StartTime, EndTime, MetricNames: ['ddos_attackBandwidth'], Interval: 'hour', ZoneIds: [account.zoneId] }` (StartTime/EndTime spanning the last 24 hours). On success return `{ available: true, data: teoResponse.Response.Data }`; on any failure/empty-mapping/API-error branch return `{ available: false }` — identical response shape convention to `cdn-traffic.js`.

    Modify `index.html`/`app.js` to complete D-01's two-card picker: add a "Security Events" card alongside the existing "CDN Traffic Stats" card, both rendered only when `data.authenticated` is true. Clicking either card: (a) fetches the matching route (`/api/data/cdn-traffic` or `/api/data/security-events`) and renders `data`/no-data exactly as Plan 03-01 established, and (b) records which source was selected as short-lived state per D-04 — append `?source=cdn-traffic` or `?source=security-events` to the current URL via `history.replaceState` (no server round-trip, no KV/cookie write) so the selection survives a page refresh within the same visit without being persisted server-side. On page load, if a `?source=` query param is already present, auto-render that card's data (skip the extra click) — this is the hook Phase 4 will build on to know which data source was chosen, without this phase needing to implement anything beyond carrying the value forward in the URL.
  </action>
  <verify>
    <automated>curl -sf -o /dev/null -w "%{http_code}" "$(cat .planning/phases/01-edgeone-deployment-foundation/DEPLOYED-URL.txt)api/data/security-events" | grep -q '^401$'</automated>
  </verify>
  <done>GET /api/data/security-events on the live deployment with no session cookie returns HTTP 401. index.html/app.js render both "CDN Traffic Stats" and "Security Events" cards only when authenticated. Selecting a card updates the URL's ?source= param via history.replaceState without any server-side write.</done>
  <acceptance_criteria>
    - `curl -sf https://{live-url}/api/data/security-events` (no cookie) returns exactly HTTP 401.
    - `edge-functions/api/data/security-events.js` uses `version: '2022-09-01'` (not `2022-01-06`) and `action: 'DescribeDDoSAttackData'`.
    - `app.js` contains a `history.replaceState` call (or equivalent URL-state update) with no corresponding `fetch` to any persistence endpoint for the selected source.
    - Both picker cards are absent from the rendered DOM when `data.authenticated` is `false`.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full live verification — DATA-01, DATA-02, DATA-03 (including the cross-tenant negative test)</name>
  <read_first>
    .planning/phases/03-tenant-scoped-data-source-selection/03-RESEARCH.md (Validation Architecture: Phase Requirements -> Test Map)
    .planning/phases/01-edgeone-deployment-foundation/DEPLOYED-URL.txt
  </read_first>
  <precondition>The KV tenant mapping and real Tencent Cloud credentials seeded during Plan 03-01's checkpoint are still in place.</precondition>
  <what-built>
    `edge-functions/api/data/security-events.js` (mirrors Plan 03-01's cdn-traffic.js pattern against `DescribeDDoSAttackData`) plus the completed two-card picker with short-lived selection-state passthrough.
  </what-built>
  <how-to-verify>
    1. **DATA-01 (CDN Traffic Stats selectable):** Log in via the test IdP. Confirm both cards render. Click "CDN Traffic Stats" — confirm the URL updates to include `?source=cdn-traffic` and the page shows either real data or "No data available".
    2. **DATA-02 (Security Events selectable):** Click "Security Events" instead — confirm the URL updates to `?source=security-events` and the page shows either real DDoS attack data or "No data available".
    3. **DATA-03 (server-side-only scoping, positive):** Confirm the data shown for both cards corresponds to the real Zone ID seeded in the KV mapping (spot-check against the EdgeOne Console's own traffic/security dashboards for that zone, if available).
    4. **DATA-03 (negative test — client input has zero effect):** With a valid session cookie, run `curl -sf -b "session=<jwt>" "https://{live-url}/api/data/cdn-traffic?zoneId=some-other-zone-id"` and `curl -sf -b "session=<jwt>" "https://{live-url}/api/data/security-events?tenant_id=some-other-tenant"`. Confirm both responses are identical to the same calls made without the extra query parameters — proving the parameter is silently ignored, not merely rejected.
    5. **Refresh persistence (D-04):** After selecting a source, refresh the browser. Confirm the same card's data re-renders automatically (URL state, not a server round-trip) without needing to click again.
  </how-to-verify>
  <resume-signal>Type "approved" once steps 1-5 pass, or describe which step failed.</resume-signal>
  <acceptance_criteria>Human has typed "approved" confirming: (a) both data sources are selectable and return tenant-scoped data or a clean no-data state, (b) a client-supplied scoping parameter provably has zero effect on either route's response, (c) the selection survives a refresh via URL state only. This checkpoint is never auto-approved regardless of `workflow.auto_advance`.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /api/data/security-events | Same boundary as Plan 03-01's cdn-traffic.js: only the verified session cookie is trusted; any client-supplied query param is inert for scoping purposes. |
| Browser URL bar (?source= state) | Untrusted but low-risk: the `source` value only selects which already-session-gated route the client calls next — it never reaches server-side scoping logic, so a tampered `?source=` value at worst causes the wrong card's data to render, not a cross-tenant leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Elevation of Privilege | edge-functions/api/data/security-events.js | critical | mitigate | Identical mitigation to T-03-01 (Plan 03-01): `ZoneIds` derives exclusively from the verified session's resolved tenant account, never from client input — re-verified here via the explicit negative test in Task 2 step 4. |
| T-03-06 | Tampering | ?source= URL query param | low | accept | The value only controls which already-authorization-gated client-side fetch fires next; tampering with it cannot access another tenant's data (that protection lives entirely server-side in the session/KV chain) — accepted as a UI-only concern with no security consequence. |

</threat_model>

<verification>
1. `curl -sf https://{live-url}/api/data/security-events` (no cookie) returns 401.
2. Live checkpoint (Task 2) confirms DATA-01, DATA-02, DATA-03 (including the explicit cross-tenant negative test) against the live deployment.
3. Both `cdn-traffic.js` and `security-events.js` use their own correct, independently-verified `version` string.
</verification>

<success_criteria>
- All three ROADMAP Phase 3 success criteria are verified true against the live deployed URL.
- DATA-01, DATA-02, DATA-03 all pass their respective automated + human-verified checks.
- No regression to Plan 03-01's cdn-traffic.js behavior or Phase 1/2's established routes.
</success_criteria>

<output>
## Artifacts this phase produces (Plan 03-02)

| Artifact | Type | Provides |
|----------|------|----------|
| `edge-functions/api/data/security-events.js` | file (new) | Session-gated route calling DescribeDDoSAttackData, tenant-scoped only |
| `index.html` / `app.js` | file (modified) | Completed two-card picker (D-01) with short-lived ?source= state passthrough (D-04) |

Create `.planning/phases/03-tenant-scoped-data-source-selection/03-02-SUMMARY.md` when done.
</output>
