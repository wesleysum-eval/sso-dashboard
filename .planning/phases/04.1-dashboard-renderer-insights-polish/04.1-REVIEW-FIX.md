---
phase: 04.1-dashboard-renderer-insights-polish
fixed_at: 2026-08-13T12:15:00Z
review_path: .planning/phases/04.1-dashboard-renderer-insights-polish/04.1-REVIEW.md
iteration: 1
findings_in_scope: 5
fixed: 5
skipped: 0
status: all_fixed
---

# Phase 4.1: Code Review Fix Report

**Fixed at:** 2026-08-13T12:15:00Z
**Source review:** .planning/phases/04.1-dashboard-renderer-insights-polish/04.1-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 5 (CR-01, CR-02, WR-01, WR-02, WR-03)
- Fixed: 5
- Skipped: 0

Info findings (IN-01, IN-02) were explicitly out of scope for this fix pass and left unfixed per instructions.

**Verification environment:** All syntax checks (`node -c`, `node --check`) and file re-reads were run inside the isolated worktree (`.codebuddy/worktrees/rf-04.1-53298-1786622769`, branch `gsd-reviewfix/04.1-53298`), which was fast-forward-merged into `main` after the last commit. Post-merge, the same file contents are present in the main checkout.

## Fixed Issues

### CR-01: `extractSeries()` can never parse the real `security-events` (DDoS) response shape

**Files modified:** `app.js`
**Commit:** `b71fe0e`
**Applied fix:** Added a third shape branch to `extractSeries()` handling `DescribeDDoSAttackData`'s `data[0].Value[].Detail[]` shape (an array under `Value`, distinct from the existing CDN-traffic `TypeValue`/`FloatTypeValue` branch). Matched the review's suggested fix closely — the surrounding code was unchanged from what the reviewer inspected.

### CR-02: The retrieval view's "✓ Saved · Read-only" badge is structurally invisible

**Files modified:** `app.js`
**Commit:** `2b6557f`
**Applied fix:** Changed `renderRetrievalView()` to hide only the prompt textarea and Generate button individually (`#prompt-textarea`, `#generate-btn`) instead of the entire `.prompt-panel` container. This leaves the heading-row (containing `#dashboard-state-badge`) visible, matching the review's suggested approach.

### WR-01: `dashboardTitle` is silently dropped on Save and never appears in the retrieval view

**Files modified:** `app.js`, `edge-functions/api/dashboard.js`
**Commit:** `a251621`
**Applied fix:**
- Added `dashboardTitle` to the client-side `draft` object; persisted the validated title from the generate response onto `draft.dashboardTitle` (previously only written directly to the DOM heading).
- Included `dashboardTitle` in `saveDashboard()`'s POST payload.
- `edge-functions/api/dashboard.js` now imports and re-validates the client-echoed title via the existing `validateDashboardTitle()` (never trusts client input as-is — same discipline as `generate.js`), storing it in the KV record only when valid.
- `dashboard/[id].js` required no change — it already returns the full stored record verbatim, so `dashboardTitle` flows through automatically once present.
- Added rendering of the retrieved `dashboardTitle` into `#prompt-panel-heading` inside `renderRetrievalView()`'s success branch (this is reachable now that CR-02 un-hides the heading).

### WR-02: `formatMetricValue()` silently ignores its `unit` parameter for two of its five format rules

**Files modified:** `app.js`
**Commit:** `7433901`
**Applied fix:** Changed the `ms-rounded` and `rate-1dp` branches to interpolate the passed-in `unit` parameter instead of the hardcoded `" ms"` / `" req/s"` suffixes, making all five format rules consistently honor the caller-supplied unit.

### WR-03: `getSourceFromUrl()`'s widened acceptance of `security-events` has no corresponding UI entry point

**Files modified:** `app.js`, `index.html`
**Commit:** `c625216`
**Applied fix:** Completed the feature (per the review's first suggested option) rather than reverting the URL-param widening, since CR-01's fix makes the security-events rendering path now functional. Added a `#card-security-events` picker card to `index.html` and a click handler in `app.js` mirroring the existing CDN Traffic Stats card exactly (sets `draft.dataSource`, reflects into the URL, fetches `/api/data/security-events`, renders via `renderChartWidget`).

## Skipped Issues

None — all 5 in-scope findings (CR-01, CR-02, WR-01, WR-02, WR-03) were fixed. IN-01 and IN-02 were out of scope for this fix pass per explicit instruction and were not attempted.

## Constraint Verification

Per the fix-pass instructions, GEN-03's constrained-generation vocabulary was checked and confirmed untouched: `git diff` of the pre-fix HEAD against the post-fix HEAD shows **zero changes** to `edge-functions/lib/generation-schema.js` — `COMPONENT_TYPES`, `METRICS_BY_SOURCE`, `INTERVALS`, `TIME_RANGES`, and `validateWidget()` remain byte-identical.

---

_Fixed: 2026-08-13T12:15:00Z_
_Fixer: gsd-code-fixer_
_Iteration: 1_
