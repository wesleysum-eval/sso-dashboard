---
phase: 04.1-dashboard-renderer-insights-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - edge-functions/lib/generation-schema.js
  - edge-functions/api/generate.js
  - app.js
  - index.html
autonomous: true
requirements: [DISPLAY-01, INSIGHT-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A generated dashboard widget's title displays a human-readable metric label (e.g. 'Outbound Traffic'), never a raw teo metric code, whenever the LLM's own title is empty/weak."
    - "Stat-card and table values display with their correct unit (bytes/bandwidth/requests/ms/req-s per METRIC_LABELS' format rule), never a bare unitless number."
    - "Chart and table timestamps display in the UI-SPEC's 'Aug 13' / 'Aug 13, 3:00 PM' format, never a raw ISO string or locale-dependent toLocaleString() string."
    - "At most one stat-card widget per dashboard (the first one in the LLM's returned order) shows hero-metric accent styling; a line-chart/bar-chart widget whose single highest point exceeds 2x its series mean shows a spike-callout; every line-chart/bar-chart widget shows a computed min/max/avg/trend summary-insight sentence."
    - "A generated dashboard displays a human-readable dashboardTitle when the LLM supplies a valid one, falling back to 'Your Dashboard' after the first generation if absent/invalid — the LLM's raw string is never executed as code and never used for anything beyond display."
    - "GEN-03's closed component/query vocabulary is untouched: COMPONENT_TYPES, METRICS_BY_SOURCE, INTERVALS, TIME_RANGES, and validateWidget()'s fail-closed enum-only behavior remain byte-identical to Phase 4 — this phase adds a sibling validator (validateDashboardTitle), never modifies the existing one."
  artifacts:
    - path: "edge-functions/lib/generation-schema.js"
      provides: "METRIC_LABELS (metric code -> {label, unit, format} for all 14 metric codes) and validateDashboardTitle() — both new exports, existing exports unchanged"
    - path: "edge-functions/api/generate.js"
      provides: "LLM response contract changed from a bare JSON array to { dashboardTitle, widgets }; final route response includes a validated dashboardTitle field"
    - path: "app.js"
      provides: "formatMetricValue(), formatTimestamp(), computeStats(), computeTrend(), detectSpike() pure functions; extractSeries()/widgetCardShell()/renderStatCardWidget()/renderChartWidget()/renderTableWidget()/renderWidgets() extended to use them; dashboardTitle displayed in .prompt-panel h2"
    - path: "index.html"
      provides: ".widget-card.is-hero, .spike-callout, .insight-summary CSS rules, built from existing :root tokens only"
  key_links:
    - from: "app.js"
      to: "edge-functions/lib/generation-schema.js"
      via: "app.js cannot import edge-functions/ (no module system) — app.js's own METRIC_LABELS constant mirrors generation-schema.js's export verbatim; both consume/produce the same widget.metric code strings"
      pattern: "METRIC_LABELS"
    - from: "edge-functions/api/generate.js"
      to: "edge-functions/lib/generation-schema.js"
      via: "import { validateDashboardTitle } — every LLM-produced dashboardTitle must pass this before being included in the response, exactly like validateWidget() already gates every widget"
      pattern: "validateDashboardTitle\\("
    - from: "app.js renderChartWidget()"
      to: "app.js detectSpike()/computeStats()/computeTrend()"
      via: "computed entirely from the already-fetched, already-validated widget.data series — no new fetch, no new LLM call"
      pattern: "detectSpike\\("
---

<objective>
Fix the D-01 root-cause rendering bug (raw `teo` metric codes as widget titles, unitless values, unformatted timestamps) and layer deterministic, computed-only insight framing (hero-metric emphasis, spike-callout, summary-insight sentence, LLM-supplied-but-validated dashboard title) on top of Phase 4's already-fetched, already-validated widget data — without touching GEN-03's locked closed-enum generation vocabulary (`COMPONENT_TYPES`/`METRICS_BY_SOURCE`/`INTERVALS`/`TIME_RANGES`/`validateWidget()`) in any way. Per `04.1-CONTEXT.md` D-02/D-03, the rejected freeform-HTML-generation direction is explicitly NOT revisited here; every new insight element is computed client-side from data the server already validated and fetched, never a new widget type the LLM can invent.

Purpose: Phase 4 proved the prompt -> generate -> refine -> save -> retrieve pipeline works end-to-end and is live-verified (GEN-01..04, SAVE-01 all complete). This phase makes the resulting dashboard actually legible — the user-reported "hard to interpret" complaint traces directly to `widgetCardShell()`'s `widget.metric` title fallback and the complete absence of any label/unit/timestamp formatting anywhere in the codebase (04.1-CONTEXT.md D-01, confirmed via direct code read, not speculation).

Output: `METRIC_LABELS`/`validateDashboardTitle()` in `generation-schema.js`; a `{ dashboardTitle, widgets }` response contract from `/api/generate`; formatting/computation helpers and their wired render call sites in `app.js`; three new CSS rules in `index.html` built exclusively from existing `:root` tokens.
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-dashboard-renderer-insights-polish/04.1-CONTEXT.md
@.planning/phases/04.1-dashboard-renderer-insights-polish/04.1-PATTERNS.md
@.planning/phases/04.1-dashboard-renderer-insights-polish/04.1-UI-SPEC.md
@.planning/phases/04-prompt-driven-dashboard-generation-save/04-CONTEXT.md
@edge-functions/lib/generation-schema.js
@edge-functions/lib/metric-lookup.js
@edge-functions/api/generate.js
@app.js
@index.html
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Metric labels, units, and formatted timestamps — end-to-end root-cause fix (D-01/D-04)</name>
  <read_first>
    edge-functions/lib/generation-schema.js (METRICS_BY_SOURCE, validateWidget)
    edge-functions/lib/metric-lookup.js (ACTION_BY_SOURCE — the fixed-lookup-table shape METRIC_LABELS must copy)
    app.js (extractSeries, widgetCardShell, renderPlaceholder, renderChartWidget, renderStatCardWidget, renderTableWidget, the cdnTrafficCard click handler's renderChartWidget call)
    .planning/phases/04.1-dashboard-renderer-insights-polish/04.1-UI-SPEC.md (Data Display Contract: Metric Label and Unit Contract, Numeric Formatting Rules, Timestamp Format Contract)
    .planning/phases/04.1-dashboard-renderer-insights-polish/04.1-PATTERNS.md (app.js section: extractSeries/widgetCardShell/renderStatCardWidget code excerpts and exact fix locations)
  </read_first>
  <files>edge-functions/lib/generation-schema.js, app.js</files>
  <action>
    In generation-schema.js, add export METRIC_LABELS: a flat object literal (same "code string maps to fixed metadata object, server-owned constant" shape as ACTION_BY_SOURCE) keyed by every one of the 14 metric codes already present in METRICS_BY_SOURCE (10 cdn-traffic + 4 security-events), each value exactly { label, unit, format } per the UI-SPEC's Metric Label and Unit Contract table: l7Flow_outFlux -> Outbound Traffic/Bytes/bytes-binary, l7Flow_inFlux -> Inbound Traffic/Bytes/bytes-binary, l7Flow_flux -> Total Traffic/Bytes/bytes-binary, l7Flow_outBandwidth -> Outbound Bandwidth/bps/bandwidth-decimal, l7Flow_inBandwidth -> Inbound Bandwidth/bps/bandwidth-decimal, l7Flow_bandwidth -> Total Bandwidth/bps/bandwidth-decimal, l7Flow_request -> Request Count/requests/integer-grouped, l7Flow_avgResponseTime -> Avg Response Time/ms/ms-rounded, l7Flow_avgFirstByteResponseTime -> Avg First-Byte Time/ms/ms-rounded, l7Flow_requestRate -> Request Rate/req per s/rate-1dp, ddos_attackMaxBandwidth -> Peak Attack Bandwidth/bps/bandwidth-decimal, ddos_attackMaxPackageRate -> Peak Attack Packet Rate/pps/integer-grouped, ddos_attackBandwidth -> Attack Bandwidth/bps/bandwidth-decimal, ddos_attackPackageRate -> Attack Packet Rate/pps/integer-grouped.

    In app.js, add a client-side METRIC_LABELS constant mirroring generation-schema.js's export verbatim (app.js has no module system and cannot import from edge-functions/, per this project's existing plain-script convention — this is a deliberate content duplication of a small closed constant table, not a new architecture; add a one-line comment above it noting it must be kept in sync with edge-functions/lib/generation-schema.js's METRIC_LABELS by hand).

    Add formatMetricValue(value, formatRule, unit) in app.js implementing the 5 Numeric Formatting Rules exactly: 'bytes-binary' (value under 1024 renders "{value} B"; under 1024 squared renders "{value/1024 to 1dp} KB"; under 1024 cubed renders "{value/1024^2 to 1dp} MB"; else "{value/1024^3 to 1dp} GB"), 'bandwidth-decimal' (value under 1000 renders "{value} bps"; under 1e6 renders "{value/1000 to 1dp} Kbps"; under 1e9 renders "{value/1e6 to 1dp} Mbps"; else "{value/1e9 to 1dp} Gbps"), 'integer-grouped' (Math.round(value).toLocaleString() plus a space plus the passed-in unit string), 'ms-rounded' (Math.round(value) plus " ms"), 'rate-1dp' (value.toFixed(1) plus " req/s"). Zero values render as "0 {unit}" (never blank) for every rule. Read the unit string from METRIC_LABELS at each call site (never hardcode "requests" vs "pps" inside formatMetricValue itself).

    Add formatTimestamp(rawValue, interval) in app.js: if interval is 'hour', format as month-abbreviation, day, comma, 12-hour time with AM/PM (e.g. "Aug 13, 3:00 PM"); if interval is 'day', format as month-abbreviation and day only (e.g. "Aug 13"); default to the 'hour' format if interval is missing or unrecognized. Accept both a Unix-seconds number (multiply by 1000 before constructing a Date) and a pre-existing Date-parseable string.

    Modify extractSeries(data, interval) to accept a second interval parameter and replace both existing label-assignment expressions (the `new Date(point.Timestamp * 1000).toLocaleString()` branch and the raw `point.Time ?? point.time ?? ''` passthrough branch) with a call to formatTimestamp(point.Timestamp ?? point.Time ?? point.time, interval). Update every call site of extractSeries — inside renderChartWidget, renderStatCardWidget, renderTableWidget — to pass widget.interval as the second argument. Update the cdnTrafficCard click handler's inline renderChartWidget call (which currently passes a literal object with title/metric/data fields and no interval field) to add an interval field set to 'hour' to that literal, matching Phase 3's hourly cdn-traffic.js query cadence.

    Modify widgetCardShell(widget) so both the title element's textContent and its title attribute compute the fallback as METRIC_LABELS keyed by widget.metric, reading its label property, falling back to widget.metric itself if the lookup misses (never the bare widget.metric alone) whenever widget.title is falsy. Apply the identical METRIC_LABELS-backed fallback to renderChartWidget's Chart.js dataset label field (currently reads widget.title with a widget.metric fallback).

    Modify renderStatCardWidget so its displayed value routes through formatMetricValue, passing the computed total, the format rule from METRIC_LABELS keyed by widget.metric, and the unit from the same lookup — instead of the current bare rounded-number string.

    Modify renderTableWidget's value column so each row's value cell routes through the same formatMetricValue call, using the widget's own format rule and unit for every row.
  </action>
  <acceptance_criteria>
    - generation-schema.js exports METRIC_LABELS with exactly 14 keys, one per METRICS_BY_SOURCE entry, each shaped as an object with label, unit, and format properties.
    - app.js's widgetCardShell and renderChartWidget's dataset label both use a METRIC_LABELS-backed fallback instead of the bare widget.metric string.
    - renderStatCardWidget and renderTableWidget both display a unit-suffixed, format-rule-correct string, never a bare unformatted number.
    - extractSeries's two label-assignment branches both route through formatTimestamp; no remaining code path assigns a raw ISO/locale-string label directly.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "METRIC_LABELS\[widget.metric\]" app.js | grep -qv '^0$' && grep -c "l7Flow_outFlux" edge-functions/lib/generation-schema.js | grep -qv '^0$' && node -e "const fs=require('fs');const src=fs.readFileSync('edge-functions/lib/generation-schema.js','utf8');const m=src.match(/METRIC_LABELS\s*=\s*\{[\s\S]*?\n\};/);if(!m){process.exit(1)}const keys=(m[0].match(/^\s*'[^']+':/gm)||[]).length;process.exit(keys===14?0:1)"</automated>
    <human-check>
      <test>Log in, select CDN Traffic Stats, generate a dashboard from a prompt like "show outbound traffic over the last 7 days".</test>
      <expected>Widget titles read human-readable labels (e.g. "Outbound Traffic"), never a raw code like "l7Flow_outFlux". Stat-card values show a unit suffix (e.g. "2.6 GB" or "142 ms"), never a bare number. Chart x-axis labels and the table "Time" column show "Aug 13" / "Aug 13, 3:00 PM"-style formatting, never a raw ISO timestamp or a long locale string like "8/13/2026, 3:00:00 PM".</expected>
    </human-check>
  </verify>
  <done>METRIC_LABELS covers all 14 metric codes; a live-generated dashboard shows human-readable titles, unit-suffixed values, and formatted timestamps end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Computed insights — hero-metric, spike-callout, summary-insight (D-03/D-04, resolves D-07)</name>
  <read_first>
    .planning/phases/04.1-dashboard-renderer-insights-polish/04.1-UI-SPEC.md (Insight Computation Contract: Hero-metric selection D-UI-12, Spike-callout D-UI-13, Summary-insight sentence D-UI-14, Component and Layout Notes)
    .planning/phases/04.1-dashboard-renderer-insights-polish/04.1-PATTERNS.md (No Analog Found table; renderChartWidget/widgetCardShell excerpts)
    app.js (renderWidgets, renderWidget, renderChartWidget, renderStatCardWidget — post-Task-1 state)
    index.html (:root tokens block, .widget-card/.stat-card-value rules, .tenant-badge shape reference)
  </read_first>
  <precondition>Task 1's formatMetricValue() and formatTimestamp() exist in app.js (same plan, prior task) — this task's spike-callout/summary-insight sentences reuse them for every interpolated number/timestamp rather than introducing a second formatting convention.</precondition>
  <files>app.js, index.html</files>
  <action>
    In index.html's existing single style block, add a `.widget-card.is-hero` rule (border-left set to 4px solid var(--color-primary), overriding only that one edge of the existing .widget-card border) and a scoped `.widget-card.is-hero .stat-card-value` rule (color set to var(--color-primary)) — every other .widget-card property inherits unchanged. Add a `.spike-callout` rule (font-size 13px, font-weight 400, color var(--color-text), margin-top 12px) with a nested `.spike-callout .spike-value` rule (font-weight 600, color var(--color-primary)). Add an `.insight-summary` rule (font-size 13px, font-weight 400, color var(--color-text-muted), margin-top 8px). All three rules use only the existing :root tokens already declared at the top of this file — zero new hex values.

    In app.js, add computeStats(series): returns an object with min, max, and avg properties — the minimum, maximum, and arithmetic mean of the series' value fields.

    Add computeTrend(series): split series into a first half and second half by index (using the floor of half the series length as the split point); compute the average value of each half; if the second half's average is more than 5 percent higher than the first half's, return 'up'; if more than 5 percent lower, return 'down'; otherwise return 'flat'.

    Add detectSpike(series): compute the arithmetic mean of all series values; find the single point with the highest value; if that point's value exceeds twice the mean, return an object containing that point and a multiplier (the point's value divided by the mean, rounded to 1 decimal place); otherwise return null. Never flag more than one point per call.

    Modify renderWidgets(widgets) to scan widgets in order and find the index of the FIRST entry whose componentType is 'stat-card' — if found, pass an isHero flag through to that one widget's render call so renderStatCardWidget can add the 'is-hero' class to that one card's root element. If no stat-card widget exists in the array, apply no hero treatment to any card — never promote a chart or table widget instead. The isHero flag must be threaded through the full existing call chain: renderWidgets(widgets) calls renderWidget(widget) (the singular per-widget dispatcher that switches on componentType to call renderChartWidget/renderStatCardWidget/renderTableWidget) — renderWidget's own signature must be extended to accept and forward this same isHero parameter to its internal renderStatCardWidget call site only (chart/table call sites simply ignore/omit it). renderStatCardWidget must accept and honor this optional isHero parameter.

    Modify renderChartWidget(widget, chartType): after the existing Chart construction call and before returning the card, call detectSpike using the same series array already extracted via extractSeries earlier in this function — if it returns a non-null result, build a `.spike-callout` div using createElement and textContent only (never innerHTML): the sentence reads "Spike detected: " (prefixed with the warning glyph) followed by the flagged point's formatted timestamp (via formatTimestamp, using the point's original raw timestamp value and widget.interval), then " — ", then the flagged point's formatted value (via formatMetricValue with the widget's own format rule and unit), then " (", then a nested span with class spike-value containing the multiplier text followed by " average", then a closing ")" — the multiplier text uses the "×" multiplication sign (U+00D7), not the ASCII letter "x" (e.g. "2.4× average"), per 04.1-UI-SPEC.md's locked Copywriting Contract spike-callout template. Append this div to the card. Then, unconditionally for line-chart and bar-chart widgets, call computeStats and computeTrend on the same series, and build an `.insight-summary` div whose textContent follows the template: the metric's label (from METRIC_LABELS, falling back to widget.metric), followed by "ranged from", the formatted min, "to", the formatted max, "averaging", the formatted avg, "and is trending", the trend word, "over this period." — each numeric value formatted via formatMetricValue using the widget's own format rule and unit. Append this div to the card after the spike-callout if present, or directly after the canvas if not.
  </action>
  <acceptance_criteria>
    - index.html contains exactly one .widget-card.is-hero rule and exactly one .spike-callout rule and exactly one .insight-summary rule, all using existing :root token var() references only (no new hex literals).
    - app.js contains exactly one function definition each for computeStats, computeTrend, and detectSpike, and exactly one call site each for computeStats/computeTrend/detectSpike inside renderChartWidget.
    - renderWidgets applies the is-hero treatment to at most one card per invocation, and only ever to a stat-card widget.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "widget-card.is-hero" index.html | grep -qv '^0$' && grep -c "function detectSpike" app.js | grep -q '^1$' && grep -c "function computeTrend" app.js | grep -q '^1$' && grep -c "detectSpike(" app.js | grep -q '^2$'</automated>
    <human-check>
      <test>Generate a dashboard whose prompt is likely to produce at least one stat-card and one line-chart/bar-chart widget over a data series with a visible spike (e.g. "show me a stat card and a chart of outbound traffic over the last 24 hours").</test>
      <expected>Exactly one stat-card (the first one returned) shows a colored left border and colored value text; if any chart widget's data has one point clearly above the rest, a spike-callout sentence appears below its chart; every chart widget shows a plain-prose summary sentence below it stating min/max/avg and a trend word (up/down/flat) — never a numeric percentage, never more than one flagged spike point per widget.</expected>
    </human-check>
  </verify>
  <done>Hero-metric styling, spike-callout, and summary-insight all render deterministically from already-fetched widget data, with zero new LLM calls and zero new widget/component types.</done>
</task>

<task type="auto">
  <name>Task 3: dashboardTitle — server-side schema extension, validation, and client display (D-05, resolves D-07 shape question)</name>
  <read_first>
    edge-functions/api/generate.js (buildSystemPrompt, callAiGateway, onRequestPost's candidates parsing and response assembly)
    edge-functions/lib/generation-schema.js (validateWidget's exact fail-closed/capped-string pattern for title)
    .planning/phases/04.1-dashboard-renderer-insights-polish/04.1-UI-SPEC.md (dashboardTitle display D-UI-15, Copywriting Contract's dashboardTitle fallback row)
    .planning/phases/04-prompt-driven-dashboard-generation-save/04-CONTEXT.md (D-08 no-leak convention — any new failure branch degrades to a default, never a distinct error)
    app.js (the generateBtn click handler's success branch, index.html's .prompt-panel h2)
    index.html (.prompt-panel h2 markup)
  </read_first>
  <precondition>Task 1's METRIC_LABELS/formatting work has landed in the same plan (Task 1 precedes this task in wave order) — this task does not depend on it functionally, but both tasks touch generation-schema.js and should not race on that file's content.</precondition>
  <files>edge-functions/lib/generation-schema.js, edge-functions/api/generate.js, app.js, index.html</files>
  <reversibility rating="reversible">Changing the LLM's top-level response shape from a bare JSON array to an object with dashboardTitle and widgets fields is a server-internal parsing change behind generate.js's own response-assembly code — no external contract or other phase reads this shape directly, so it can be adjusted again later without a migration.</reversibility>
  <action>
    In generation-schema.js, add export validateDashboardTitle(raw): mirrors validateWidget's title field treatment exactly (String(raw or empty string).slice(0, 120)) but returns null instead of an empty string when raw is not a usable value (undefined, not a string, or empty after trimming) — matching this file's existing "return null on any failure, never throw" discipline, so the caller decides the fallback display text rather than this function inventing one.

    In generate.js: change buildSystemPrompt's system-prompt lines array to (a) change the "You must return ONLY a JSON array" instruction to describe a top-level JSON object shaped `{ dashboardTitle, widgets }` where widgets is the array of widget objects exactly as before, and (b) add one new line describing dashboardTitle as a short, free-text, human-readable title for the whole dashboard (not used for anything else) — worded in the same style as the existing title-is-free-text line for individual widgets. Update callAiGateway's caller (onRequestPost) so that after parsing the model's JSON response, it reads `candidates.widgets` (an array) instead of treating the parsed JSON itself as the array — if the parsed JSON is not an object or its widgets field is not an array, treat this identically to the existing "not an Array" failure branch (call generationFailed(), same as today, no new error shape). Extract `candidates.dashboardTitle` and pass it through validateDashboardTitle() once, independent of the per-widget validation loop (dashboard-level, not per-widget). In the final assembled response, add the validated dashboardTitle (or omit the field entirely if validation returned null — never send an empty string or a distinct error) alongside the existing widgets and prompt fields.

    In app.js's generateBtn click handler's success branch (where it currently sets draft.spec/draft.data and calls renderWidgets), add: if body.dashboardTitle is present and is a non-empty string, set draft.dashboardTitle to it and update the `.prompt-panel h2` element's textContent to that string; otherwise set the heading's textContent to the fallback "Your Dashboard" — this fallback only takes effect once at least one dashboard has been generated (before the first generation, leave the existing "Generate a dashboard" heading text untouched — do not run this update logic outside the success branch).

    Confirm no change is needed to index.html's markup itself (the existing `.prompt-panel h2` element is reused as-is, only its textContent changes at runtime) — if the executor finds the h2 lacks a stable id/selector for app.js to target reliably, add one (e.g. id="prompt-panel-heading") as the only markup change this task makes.

    Also in generate.js: update the existing retry-correction string (built inside the "other parse/fetch error — retry once with an explicit correction" branch, currently reading `` `${systemPrompt}\nYour previous response was not valid JSON matching the schema. Return ONLY the JSON array, nothing else.` ``) so its wording matches this task's new `{ dashboardTitle, widgets }` object shape instead of instructing the model to return a bare JSON array — e.g. "Return ONLY the JSON object with dashboardTitle and widgets fields, nothing else." This string must never again tell the model to return "the JSON array" once this task lands, since that would contradict the new top-level object contract and could cause the retried response to fail the new candidates.widgets parsing.
  </action>
  <acceptance_criteria>
    - generation-schema.js exports validateDashboardTitle, following the same return-null-never-throw discipline as validateWidget.
    - generate.js's onRequestPost reads the LLM's dashboardTitle field from a `{ dashboardTitle, widgets }` shaped response, and validates it via validateDashboardTitle before including it in the final response; a missing/invalid dashboardTitle field never triggers generationFailed() on its own.
    - app.js updates the .prompt-panel heading's textContent only inside the generate success branch, defaulting to "Your Dashboard" when dashboardTitle is absent/invalid, and never touches that heading before the first successful generation.
    - generate.js's retry-correction string describes the `{ dashboardTitle, widgets }` object shape, never instructs the model to return "the JSON array".
  </acceptance_criteria>
  <verify>
    <automated>grep -c "validateDashboardTitle" edge-functions/lib/generation-schema.js | grep -qv '^0$' && grep -c "validateDashboardTitle" edge-functions/api/generate.js | grep -qv '^0$' && grep -c "dashboardTitle" app.js | grep -qv '^0$' && grep -c "Your Dashboard" app.js | grep -qv '^0$'</automated>
    <human-check>
      <test>Generate a dashboard normally, observing the heading above the prompt textarea before and after generation completes.</test>
      <expected>Before the first generation, the heading still reads "Generate a dashboard" unchanged. After a successful generation, the heading updates to either an LLM-supplied short title or the fallback "Your Dashboard" — never a blank heading, never raw JSON, never a validation error message.</expected>
    </human-check>
  </verify>
  <done>The generation response contract carries a validated, capped, display-only dashboardTitle; the client reflects it (or its fallback) in the existing heading element without any new error state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response -> generate.js | The AI Gateway's JSON response (widgets array + new dashboardTitle field) is untrusted model output — data, never code, exactly as established in Phase 4. |
| generate.js -> client (app.js) | The assembled `{ dashboardTitle, widgets, prompt }` response crosses into the browser and is rendered via textContent/createElement only. |
| widget.data (teo response) -> app.js render functions | Already-fetched, already-validated per-widget data; this phase's new detectSpike/computeStats/computeTrend/formatMetricValue/formatTimestamp functions consume it read-only, producing new derived strings for display. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-01 | Tampering | edge-functions/api/generate.js (dashboardTitle field) | medium | mitigate | dashboardTitle is validated via validateDashboardTitle() (length-capped string or null) before being included in any response — never executed, never used to construct a query, mirroring validateWidget's existing title treatment. Task 3. |
| T-04.1-02 | Information Disclosure | edge-functions/api/generate.js (top-level response shape change) | low | accept | Changing from a bare array to `{ dashboardTitle, widgets }` is an internal parsing contract between this route and app.js only; no other phase or external consumer reads this shape, so the change carries no cross-boundary disclosure risk. |
| T-04.1-03 | Elevation of Privilege / scope creep | app.js render functions (new computed insight functions) | low | accept | detectSpike/computeStats/computeTrend/formatMetricValue/formatTimestamp are pure functions operating only on data already fetched and validated by Phase 4's generate.js — they introduce no new fetch, no new LLM call, and cannot expand the closed component/query vocabulary (COMPONENT_TYPES/METRICS_BY_SOURCE/INTERVALS/TIME_RANGES/validateWidget() are read-only in this plan, never modified). |
| T-04.1-04 | Tampering | app.js METRIC_LABELS (client-side duplicate) | low | accept | The client-side METRIC_LABELS constant is a display-only lookup table (label/unit/format strings), never used to construct or validate a server-bound query — a drifted or stale client copy only risks a cosmetic label mismatch, never a security boundary violation, since generation-schema.js's server-side copy remains the sole enforcement point for validateWidget(). |

</threat_model>

<verification>
- All three tasks' automated `<verify>` checks pass (grep/node assertions on generation-schema.js/app.js/index.html content, plus the live human-check walkthroughs).
- A fresh live generation against the deployed URL shows: human-readable widget titles, unit-suffixed values, formatted timestamps, at most one hero stat-card, spike-callouts only where a genuine 2x-mean point exists, a summary-insight sentence on every chart widget, and a dashboardTitle (or its fallback) in the prompt-panel heading.
- `grep -c "COMPONENT_TYPES = \['line-chart'" edge-functions/lib/generation-schema.js` still returns exactly the original 4-value array — confirms GEN-03's vocabulary was not touched by this phase.
</verification>

<success_criteria>
- Phase 4.1's goal (ROADMAP.md) is met: generated/saved dashboards render human-readable labels, units, formatted timestamps, and computed hero-metric/spike/summary insight framing, without reopening GEN-03's constrained-generation trust boundary.
- Both Plan 04.1-01 (this plan) and Plan 04.1-02 (Save/Preview UI polish) are executed and live-verified.
</success_criteria>

<output>
Create `.planning/phases/04.1-dashboard-renderer-insights-polish/04.1-01-SUMMARY.md` when done
</output>
