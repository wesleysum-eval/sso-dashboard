---
phase: 04.2-generation-pipeline-reliability-observability
plan: 02
type: execute
wave: 2
depends_on: ["04.2-01"]
files_modified:
  - edge-functions/api/generate.js
  - edge-functions/api/dashboard.js
  - edge-functions/api/dashboard/[id].js
autonomous: true
requirements: [SAVE-01, GEN-02]

estimate:
  tokens: 7000
  raw_tokens: 5500
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every generation attempt, save attempt, and retrieve attempt emits exactly one structured console.log(JSON.stringify({...})) call, placed after the verifySession() 401 branch, per 04-AI-SPEC.md Section 7's event shape."
    - "No log call anywhere in generate.js/dashboard.js/dashboard/[id].js ever includes secretId, secretKey, MAKERS_MODELS_KEY, raw prompt text, raw LLM content, teoResponse.Response.Error, or any widget's data values."
    - "dashboard/[id].js logs a hit/miss outcome for every retrieve attempt (identical log call for both 'missing key' and 'wrong tenant' causes, matching the existing D-06 no-distinguish-cause response discipline) — this is the raw signal a repeated-miss-from-same-tenant probing detector would consume later, not a detector itself."
  artifacts:
    - "edge-functions/api/generate.js — console.log(JSON.stringify({event:'generation_result', ...})) call site"
    - "edge-functions/api/dashboard.js — console.log(JSON.stringify({event:'save', ...})) call sites (success and kv_put_failed outcomes)"
    - "edge-functions/api/dashboard/[id].js — console.log(JSON.stringify({event:'retrieve', ...})) call site (hit/miss)"
  key_links:
    - "generate.js: after Plan 01's widgets/requestedCount/validCount are computed, immediately before the final return new Response(...) -> one console.log call carrying tenantId/dataSource/requestedWidgetCount/validatedWidgetCount/timestamp"
    - "dashboard.js: inside the existing try/catch around my_kv.put() -> one console.log call on each of the two outcome branches (success right before the final return, kv_put_failed right before return saveFailed())"
    - "dashboard/[id].js: immediately after const raw = await my_kv.get(...), before the if (!raw) branch -> one console.log call carrying outcome: raw ? 'hit' : 'miss'"
---

<objective>
Close production's observability gap (D-02) by adding structured `console.log(JSON.stringify({...}))` calls at the exact generation/save/retrieve decision points `04-AI-SPEC.md` Section 7 already specifies — this is implementation-only, the design already exists. No new dependency, no new trust boundary; this phase's third-priority item per CONTEXT.md's locked D-05 order (after D-03's concurrency fix and D-04's partial-success signal, both landed in Plan 01).

Purpose: Production is currently fully "dark" — zero logging exists in `generate.js`/`dashboard.js`/`dashboard/[id].js`. Structured logs enable tracking retry rate, zero-valid-widget rate, and cross-tenant probing attempts (repeated `not_found` from the same session against different `dashboard_id`s) via EdgeOne's platform log viewer.
Output: One `console.log(JSON.stringify({...}))` call at each of the three routes' key decision points, every call reviewed against the no-leak allowlist-by-omission discipline.
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-generation-pipeline-reliability-observability/04.2-CONTEXT.md
@.planning/phases/04.2-generation-pipeline-reliability-observability/04.2-PATTERNS.md
@.planning/phases/04-prompt-driven-dashboard-generation-save/04-AI-SPEC.md
@.planning/phases/04.2-generation-pipeline-reliability-observability/04.2-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Structured generation-result logging in generate.js (D-02)</name>
  <files>edge-functions/api/generate.js</files>
  <read_first>
    edge-functions/api/generate.js (after Plan 01's changes — the final response-assembly block: `const responseBody = { widgets, prompt, requestedCount, validCount, ... }` and the `return new Response(JSON.stringify(responseBody), ...)` line; also `payload` from `verifySession()` and `dataSource` from the request body, both already in scope);
    .planning/phases/04.2-generation-pipeline-reliability-observability/04.2-PATTERNS.md ("D-02 structured logging — insertion point and no-leak comment convention" section — use this exact object shape and insertion point);
    .planning/phases/04.2-generation-pipeline-reliability-observability/04.2-RESEARCH.md ("Structured log event shape (D-02, per 04-AI-SPEC.md Section 7 verbatim)" and "Pitfall 3: Logging the wrong things per D-08's no-leak convention")
  </read_first>
  <action>
    Add exactly one `console.log(JSON.stringify({...}))` call in `generate.js`, placed immediately before the final `return new Response(JSON.stringify(responseBody), ...)` statement (after `widgets`/`requestedCount`/`validCount`/`dashboardTitle` are all finalized, so every field being logged is already known). The logged object's fields: `event: 'generation_result'`, `tenantId: payload.tenant_id`, `dataSource`, `requestedWidgetCount: candidates?.widgets?.length ?? null`, `validatedWidgetCount: widgets.length`, `timestamp: new Date().toISOString()`. Precede the call with a one-line comment copied verbatim: `// NEVER logged: raw prompt text, raw LLM content, MAKERS_MODELS_KEY, dashboard data values.` — this is not decorative, it is the explicit allowlist-by-omission check every future edit to this call site must pass (RESEARCH.md Pitfall 3's own recommended mitigation: copy the constraint into the code, not just the design doc).

    Do NOT add this logging inside either of the two `generationFailed()` early-return branches (malformed body, unknown dataSource, parse-retry-exhausted, zero-valid-widgets, missing tenant account, zero-teo-fetched-widgets) — this phase's minimum scope per CONTEXT.md D-02 is "at minimum: generation attempt (dataSource, widget count requested/valid, retry used y/n), save attempt, retrieve attempt" but the concrete event shape given in both AI-SPEC.md Section 7 and RESEARCH.md's own Code Examples is the single `generation_result` shape at the success path — add exactly this one call site, matching the design doc's own scope, not an expanded set of failure-branch log calls not specified there.
  </action>
  <verify>
    <automated>node --check edge-functions/api/generate.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "event: 'generation_result'" edge-functions/api/generate.js` matches exactly once
    - `grep -B2 "event: 'generation_result'" edge-functions/api/generate.js` shows the `// NEVER logged: ...` comment line directly above the `console.log` call
    - `grep -A8 "event: 'generation_result'" edge-functions/api/generate.js | grep -Ec "secretId|secretKey|MAKERS_MODELS_KEY|prompt:|content:|teoResponse"` returns 0 — the logged object contains none of these keys
    - `node --check edge-functions/api/generate.js` exits 0
  </acceptance_criteria>
  <done>generate.js emits exactly one structured JSON log line per generation attempt reaching the success path, carrying only tenantId/dataSource/requestedWidgetCount/validatedWidgetCount/timestamp — never a secret, prompt, or LLM content.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Structured save/retrieve logging in dashboard.js and dashboard/[id].js (D-02)</name>
  <files>edge-functions/api/dashboard.js, edge-functions/api/dashboard/[id].js</files>
  <read_first>
    edge-functions/api/dashboard.js (full file — the `try { await my_kv.put(...); } catch { return saveFailed(); }` block and the final `return new Response(JSON.stringify({ dashboardId }), ...)` line, plus `payload.tenant_id` already in scope);
    edge-functions/api/dashboard/[id].js (full file — `const raw = await my_kv.get(...)` and the immediately following `if (!raw) { return notFound(); }` block, plus `payload.tenant_id`/`params.id` already in scope);
    .planning/phases/04.2-generation-pipeline-reliability-observability/04.2-PATTERNS.md ("edge-functions/api/dashboard.js (route, CRUD-write) — D-02" and "edge-functions/api/dashboard/[id].js (route, CRUD-read) — D-02" sections — use these exact object shapes/insertion points);
    .planning/phases/04.2-generation-pipeline-reliability-observability/04.2-CONTEXT.md (D-02's explicit "repeated not_found from the same session against different dashboard_ids" probing-detection framing — this task logs the raw hit/miss signal, it does not build a detector)
  </read_first>
  <action>
    `dashboard.js`: add two `console.log(JSON.stringify({...}))` calls, both with `event: 'save'`, `tenantId: payload.tenant_id`, `timestamp: new Date().toISOString()`, and an `outcome` field that differs per branch: (a) inside the existing `catch { return saveFailed(); }` block, log `outcome: 'kv_put_failed'` immediately before `return saveFailed();`; (b) on the success path, log `outcome: 'success'` immediately before the final `return new Response(JSON.stringify({ dashboardId }), ...)`. Precede each with the same `// NEVER logged: spec/data contents, prompt text, secretId/secretKey.` comment line (PATTERNS.md's exact wording).

    `dashboard/[id].js`: add exactly one `console.log(JSON.stringify({...}))` call, placed immediately after `const raw = await my_kv.get(...)` and BEFORE the `if (!raw) { return notFound(); }` branch — this single call site must fire for BOTH the hit and miss cases (that is the point: hit/miss is itself the signal, not a separate branch per outcome). Fields: `event: 'retrieve'`, `tenantId: payload.tenant_id`, `dashboardId: params.id`, `outcome: raw ? 'hit' : 'miss'`, `timestamp: new Date().toISOString()`. Precede with `// NEVER logged: dashboard spec/data contents, prompt text.` (PATTERNS.md's exact wording). Do not add any additional detection logic (no in-memory counter, no KV-backed probe tracker) — this phase's scope is emitting the raw signal into the log stream only; a human or future tooling reading the log stream is the detector, per CONTEXT.md's own framing.
  </action>
  <verify>
    <automated>node --check edge-functions/api/dashboard.js && node --check "edge-functions/api/dashboard/[id].js"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "event: 'save'" edge-functions/api/dashboard.js` returns exactly 2 (success + kv_put_failed outcomes)
    - `grep -n "outcome: 'kv_put_failed'" edge-functions/api/dashboard.js` and `grep -n "outcome: 'success'" edge-functions/api/dashboard.js` both match
    - `grep -c "event: 'retrieve'" "edge-functions/api/dashboard/[id].js"` returns exactly 1, and it appears BEFORE the `if (!raw)` line in file order (`grep -n` line number for `event: 'retrieve'` is lower than the line number for `if (!raw)`)
    - `grep -A6 "event: 'save'" edge-functions/api/dashboard.js | grep -Ec "spec|data:|prompt:"` returns 0 across both call sites combined
    - `grep -A6 "event: 'retrieve'" "edge-functions/api/dashboard/[id].js" | grep -Ec "spec|data:|prompt:"` returns 0
    - Both `node --check` commands exit 0
  </acceptance_criteria>
  <done>dashboard.js logs every save attempt's outcome (success/kv_put_failed); dashboard/[id].js logs every retrieve attempt's hit/miss outcome from a single call site covering both causes — no response-body change, no new detection logic, zero leaked spec/data/prompt content in any logged object.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application code -> EdgeOne platform log viewer | New logging surface (D-02) — every console.log call is a potential new information-disclosure vector if the no-leak allowlist is violated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-03 | Information Disclosure | New `console.log` call sites in generate.js/dashboard.js/dashboard/[id].js | high | mitigate | Every call site preceded by an explicit "NEVER logged" comment naming the exact forbidden keys (secretId/secretKey/MAKERS_MODELS_KEY/prompt/content/spec/data); verified via grep audit in each task's acceptance_criteria — no runtime scanner exists, this is the established verification style per 04-01-SUMMARY.md's coverage table precedent |
| T-04.2-04 | Information Disclosure (defensive monitoring) | dashboard/[id].js retrieve hit/miss log (cross-tenant probing detection) | medium | mitigate | Log every retrieve attempt's outcome (hit/miss) from a single call site covering both "missing key" and "wrong tenant" causes — the detection signal lives only in the log stream, never in the HTTP response (D-06's no-distinguish-cause convention is unchanged) |

</threat_model>

<verification>
- `node --check` passes on all three modified files
- Grep audits confirm exactly the specified log call counts and zero forbidden-key leakage across all five new call sites (1 in generate.js, 2 in dashboard.js, 1 in dashboard/[id].js — 4 total, not 5; correcting: generate.js=1, dashboard.js=2, dashboard/[id].js=1)
- Manual grep-based no-leak audit per 04.2-VALIDATION.md's Manual-Only Verifications table: `grep -n "console.log" edge-functions/api/generate.js edge-functions/api/dashboard.js "edge-functions/api/dashboard/[id].js"` followed by manual inspection of every logged object's keys against the never-log allowlist
</verification>

<success_criteria>
- Production is no longer "dark" — every generation-result, save-attempt, and retrieve-attempt reaching a terminal branch emits exactly one structured JSON log line
- Zero secrets, prompt text, LLM content, or dashboard data values appear in any logged object (grep-verified)
- The cross-tenant-probing detection signal (repeated retrieve misses from the same tenantId) is present in the log stream via the hit/miss field, with no change to the HTTP response shape or D-06's no-distinguish-cause guarantee
</success_criteria>

<output>
## Artifacts this phase produces (Plan 04.2-02)

| Artifact | Type | Provides |
|----------|------|----------|
| `event: 'generation_result'` | log event (new, `edge-functions/api/generate.js`) | `{ event, tenantId, dataSource, requestedWidgetCount, validatedWidgetCount, timestamp }` — one line per generation reaching the success path |
| `event: 'save'` | log event (new, `edge-functions/api/dashboard.js`) | `{ event, tenantId, outcome: 'success'\|'kv_put_failed', timestamp }` — one line per save attempt, either outcome branch |
| `event: 'retrieve'` | log event (new, `edge-functions/api/dashboard/[id].js`) | `{ event, tenantId, dashboardId, outcome: 'hit'\|'miss', timestamp }` — one line per retrieve attempt, single call site covering both hit and miss (the raw cross-tenant-probing signal) |

Create `.planning/phases/04.2-generation-pipeline-reliability-observability/04.2-02-SUMMARY.md` when done
</output>
