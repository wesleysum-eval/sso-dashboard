---
phase: 04.2-generation-pipeline-reliability-observability
plan: 03
type: execute
wave: 3
depends_on: ["04.2-02"]
files_modified:
  - evals/promptfooconfig.yaml
  - evals/provider.js
  - evals/system-prompt.txt
  - evals/testcases.yaml
  - evals/assertions/schema-compliance.js
  - evals/assertions/interval-coherence.js
  - package.json
autonomous: false
requirements: [GEN-03]
user_setup:
  - service: promptfoo (npm devDependency)
    why: "04-AI-SPEC.md Section 5 locks Promptfoo as this phase's eval tool; the package-legitimacy check flagged it [SUS] (too-new heuristic, likely a false positive per RESEARCH.md's own analysis) so a human must confirm its registry identity before install"
    env_vars: []
    dashboard_config: []

estimate:
  tokens: 12000
  raw_tokens: 9500
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The evals/ directory exists with a runnable Promptfoo config, a provider wrapping the same AI Gateway call generate.js makes, a 15-example reference dataset matching 04-AI-SPEC.md Section 5's composition, and two assertion files (schema-compliance, interval-coherence)."
    - "evals/assertions/schema-compliance.js imports validateWidget() directly from edge-functions/lib/generation-schema.js — it never re-declares the enum lists, so the eval suite cannot silently drift from the production validator."
    - "npm install --save-dev promptfoo only happens after an explicit human confirmation of the package's registry identity (repository field, license, download profile) — this checkpoint is never auto-approved regardless of workflow.auto_advance."
    - "npx promptfoo eval runs at least once against the real AI Gateway and produces a baseline evals/results/latest.json — a 100% pass rate is NOT required this phase (first-run baseline is expected to surface failures to fix later, per CONTEXT.md D-05)."
  artifacts:
    - "evals/promptfooconfig.yaml"
    - "evals/provider.js"
    - "evals/system-prompt.txt"
    - "evals/testcases.yaml"
    - "evals/assertions/schema-compliance.js"
    - "evals/assertions/interval-coherence.js"
    - "evals/results/latest.json (baseline eval run output)"
    - "package.json devDependencies.promptfoo"
  key_links:
    - "evals/promptfooconfig.yaml providers[0] -> file://evals/provider.js -> same https://ai-gateway.edgeone.link/v1/chat/completions endpoint/model generate.js calls, reimplemented for Node.js (no AbortSignal.timeout polyfill needed there)"
    - "evals/assertions/schema-compliance.js -> import { validateWidget } from '../edge-functions/lib/generation-schema.js' (direct reuse, never a re-declared copy of COMPONENT_TYPES/METRICS_BY_SOURCE/etc.)"
    - "checkpoint:human-verify (Task 2) -> npm install --save-dev promptfoo (Task 3) -> npx promptfoo eval --config evals/promptfooconfig.yaml --output evals/results/latest.json"
---

<objective>
Stand up the Promptfoo eval suite `04-AI-SPEC.md` Section 5 already designed but Phase 4 never built (D-01) — the biggest-lift, lowest-priority item in CONTEXT.md's locked D-05 order, correctly sequenced last after Plan 01's concurrency/transparency fixes and Plan 02's structured logging. All non-install authoring (config, provider, system prompt, reference dataset, assertions) happens first; `npm install --save-dev promptfoo` is gated behind a mandatory human-verify checkpoint because the package-legitimacy check flagged `promptfoo` `[SUS]` (reason: `too-new`), per RESEARCH.md's explicit instruction.

Purpose: No eval suite exists — zero automated regression coverage for GEN-03's schema/enum compliance, interval/timeRange coherence, or the other dimensions `04-AI-SPEC.md` Section 5 already specified. This phase is implementation-fidelity only; the eval design itself is not re-derived.
Output: A runnable `evals/` directory + one baseline `npx promptfoo eval` run producing `evals/results/latest.json`.
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-generation-pipeline-reliability-observability/04.2-CONTEXT.md
@.planning/phases/04.2-generation-pipeline-reliability-observability/04.2-RESEARCH.md
@.planning/phases/04.2-generation-pipeline-reliability-observability/04.2-PATTERNS.md
@.planning/phases/04.2-generation-pipeline-reliability-observability/04.2-VALIDATION.md
@.planning/phases/04-prompt-driven-dashboard-generation-save/04-AI-SPEC.md
@edge-functions/lib/generation-schema.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author evals/ — config, provider, system prompt, 15-example dataset, assertions (D-01, no install yet)</name>
  <files>evals/promptfooconfig.yaml, evals/provider.js, evals/system-prompt.txt, evals/testcases.yaml, evals/assertions/schema-compliance.js, evals/assertions/interval-coherence.js</files>
  <read_first>
    edge-functions/lib/generation-schema.js (full file — `COMPONENT_TYPES`, `INTERVALS`, `TIME_RANGES`, `METRICS_BY_SOURCE`, `validateWidget()` — confirm it is pure JS with no edge-runtime-only globals, safe to `import` from a Node.js-run script);
    edge-functions/api/generate.js (`buildSystemPrompt()` and `callAiGateway()` — the exact system-prompt text structure and AI Gateway request shape `evals/system-prompt.txt`/`evals/provider.js` must mirror);
    .planning/phases/04-prompt-driven-dashboard-generation-save/04-AI-SPEC.md ("## 5. Evaluation Strategy" section in full — Dimensions table, Eval Tooling, Reference Dataset composition/labeling — this is the design contract, do not re-derive);
    .planning/phases/04.2-generation-pipeline-reliability-observability/04.2-PATTERNS.md ("evals/assertions/schema-compliance.js" and "evals/provider.js" sections — exact code shapes to use);
    .planning/phases/04.2-generation-pipeline-reliability-observability/04.2-RESEARCH.md ("Code Examples" section, "Promptfoo config skeleton reusing generation-schema.js", and "Recommended Project Structure")
  </read_first>
  <action>
    Create the `evals/` directory with these six files, none of which require `promptfoo` to be installed to author correctly (they only need it installed to *run*, per RESEARCH.md Open Question 2's recommended sequencing):

    1. `evals/promptfooconfig.yaml` — per RESEARCH.md's "Promptfoo config skeleton" code example: `providers: [{id: file://evals/provider.js}]`, `prompts: [file://evals/system-prompt.txt]`, `tests: file://evals/testcases.yaml`, `defaultTest.assert` listing two `type: javascript` entries pointing at `file://evals/assertions/schema-compliance.js` and `file://evals/assertions/interval-coherence.js`.

    2. `evals/provider.js` — a default-exported `GatewayProvider` class with `id = () => 'edgeone-ai-gateway'` and an async `callApi(prompt, context)` method that POSTs to `https://ai-gateway.edgeone.link/v1/chat/completions` with `Authorization: Bearer ${process.env.MAKERS_MODELS_KEY}`, model `@makers/deepseek-v4-flash`, `messages: [{role:'system', content: context.vars.systemPrompt}, {role:'user', content: prompt}]`, and returns `{ output: body?.choices?.[0]?.message?.content ?? '' }` — same AI Gateway endpoint/model `generate.js`'s `callAiGateway()` uses, reimplemented for plain Node.js (no `AbortSignal.timeout` polyfill needed here — Node's native implementation is available outside the edge runtime).

    3. `evals/system-prompt.txt` — plain text mirroring `buildSystemPrompt()`'s output structure from `generate.js` (the fixed vocabulary description: `componentType`/`metric`/`interval`/`timeRange` enum lists, the "return ONLY JSON" instruction, the `dashboardTitle`+`widgets` top-level shape) — for both `cdn-traffic` and `security-events` data sources, since the eval dataset covers both.

    4. `evals/testcases.yaml` — exactly 15 test cases matching `04-AI-SPEC.md` Section 5's Reference Dataset composition verbatim: 4× clear-intent CDN traffic prompts, 3× clear-intent security-events prompts, 2× colloquial/ambiguous time-range phrasing, 2× adversarial prompts attempting an out-of-vocabulary component type or raw metric string, 2× re-prompt/refinement chains (initial prompt + follow-up with `previousSpec` context), 1× cross-tenant probing attempt (a `dashboard_id` guess against a different tenant), 1× intentionally malformed/nonsensical prompt expected to produce zero valid widgets. Each test case's `vars` must include at minimum `prompt` and `dataSource` (`cdn-traffic` or `security-events`) so `schema-compliance.js`'s `context.vars.dataSource` resolves correctly.

    5. `evals/assertions/schema-compliance.js` — per PATTERNS.md's exact shape: `import { validateWidget } from '../edge-functions/lib/generation-schema.js';` then `module.exports = (output, context) => {...}` that parses `output` as JSON (or uses it directly if already an object), extracts `parsed?.widgets` (defaulting to `[]` if not an array), checks `widgets.every((w) => validateWidget(w, context.vars.dataSource) !== null)`, and returns `{ pass, score: pass ? 1 : 0, reason }`. Never re-declare `COMPONENT_TYPES`/`METRICS_BY_SOURCE`/etc. — import only.

    6. `evals/assertions/interval-coherence.js` — a new pure function `isCoherentPairing(interval, timeRange)` (no existing analog in this codebase, author per `04-AI-SPEC.md`'s own "Interval/timeRange coherence" dimension row: PASS means the pairing yields a readable point count — roughly ≤48 points for `hour`, a sane multi-point series for `day`; e.g. `hour`+`last24h` (24 pts) is coherent, `hour`+`last30d` (~720 pts) is not, `day`+`last24h` (1 pt) is not, `day`+`last7d`/`day`+`last30d` are coherent) — wrapped in the same `module.exports = (output, context) => {...}` assertion-function shape as `schema-compliance.js`, checking every widget's `interval`/`timeRange` pair and failing if any widget is incoherent.
  </action>
  <verify>
    <automated>node --check evals/provider.js && node --check evals/assertions/schema-compliance.js && node --check evals/assertions/interval-coherence.js</automated>
  </verify>
  <acceptance_criteria>
    - `ls evals/promptfooconfig.yaml evals/provider.js evals/system-prompt.txt evals/testcases.yaml evals/assertions/schema-compliance.js evals/assertions/interval-coherence.js` lists all six files
    - `grep -n "import { validateWidget }" evals/assertions/schema-compliance.js` matches, and `grep -Ec "COMPONENT_TYPES|METRICS_BY_SOURCE|INTERVALS.*=.*\[|TIME_RANGES.*=.*\[" evals/assertions/schema-compliance.js` returns 0 (no re-declared enum arrays)
    - `grep -c "^- " evals/testcases.yaml` (or an equivalent count of top-level test-case entries) reflects 15 test cases — verify by manual inspection against the 4/3/2/2/2/1/1 composition breakdown
    - `grep -n "function isCoherentPairing" evals/assertions/interval-coherence.js` matches
    - `grep -n "ai-gateway.edgeone.link" evals/provider.js` matches the same URL `generate.js` uses
    - All three `node --check` commands exit 0
    - `package.json` and `node_modules/` are untouched by this task — no install has happened yet
  </acceptance_criteria>
  <done>The complete evals/ directory exists and is internally consistent (config references provider/prompt/testcases/assertions correctly by relative path), schema-compliance.js reuses generation-schema.js's validateWidget() directly, and none of this authoring required promptfoo to be installed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm promptfoo package identity before install (package-legitimacy SUS flag)</name>
  <read_first>
    .planning/phases/04.2-generation-pipeline-reliability-observability/04.2-RESEARCH.md ("Package Legitimacy Audit" section in full — this is where the `[SUS]`/`too-new` flag and its supporting evidence (630K weekly downloads, 3+ year old canonical GitHub repo, MIT license, no postinstall script) were originally recorded; the checks below re-verify those same findings at execution time rather than trusting a stale research snapshot)
  </read_first>
  <what-built>Task 1 authored the complete evals/ directory (config, provider, system prompt, 15-example dataset, two assertion files) — none of it required promptfoo to be installed. This checkpoint exists because RESEARCH.md's automated package-legitimacy check flagged the `promptfoo` npm package `[SUS]` (reason: `too-new` — very likely a false positive from the check tracking the latest version's publish date rather than the package's actual 3+ year age, but the protocol requires human confirmation regardless of how confident the other signals look).</what-built>
  <how-to-verify>
    Before running `npm install --save-dev promptfoo`, confirm:
    1. Run `npm view promptfoo repository.url` — confirm it resolves to `git+https://github.com/promptfoo/promptfoo.git` (or equivalent `github.com/promptfoo/promptfoo` reference).
    2. Run `npm view promptfoo license` — confirm `MIT`.
    3. Run `npm view promptfoo` (full output) — confirm weekly downloads are in the hundreds-of-thousands range and `time.created` reflects a multi-year-old project (not a package created days ago), consistent with RESEARCH.md's findings (630K weekly downloads, created 2023-05-03).
    4. Confirm no `postinstall`/`preinstall` script is listed in the package's `scripts` field via `npm view promptfoo scripts`.
    If all four checks match RESEARCH.md's findings, approve. If ANY check differs materially (different repository, different license, a postinstall script now present, or a suspiciously recent creation date), do NOT approve — stop and report the discrepancy instead.
  </how-to-verify>
  <acceptance_criteria>
    - (a) `node_modules/promptfoo/package.json`'s `repository` field (once installed) matches `github.com/promptfoo/promptfoo` — pre-install, this is checked via `npm view promptfoo repository.url` instead
    - (b) `npm view promptfoo license` outputs exactly `MIT`
    - (c) `npm view promptfoo scripts` shows no `postinstall`/`preinstall` key
    - (d) The human has explicitly typed "approved" in response to this checkpoint, confirming all three checks (a)-(c) passed — a missing or non-"approved" response means Task 3 (the actual `npm install`) must not proceed
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with `npm install --save-dev promptfoo`, or describe the discrepancy found.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Install promptfoo + run baseline eval (D-01)</name>
  <files>package.json, evals/results/latest.json</files>
  <read_first>
    package.json (current `dependencies` block — add `promptfoo` under a new `devDependencies` key, do not add it to `dependencies`);
    .planning/phases/04.2-generation-pipeline-reliability-observability/04.2-VALIDATION.md ("Sampling Rate" section — "Before /gsd:verify-work: npx promptfoo eval run once producing a baseline evals/results/latest.json (100% pass rate NOT required this phase)")
  </read_first>
  <action>
    Only after Task 2's checkpoint is explicitly approved: run `npm install --save-dev promptfoo` at the repo root. Confirm `package.json` now has a `devDependencies.promptfoo` entry. Then run `npx promptfoo eval --config evals/promptfooconfig.yaml --output evals/results/latest.json` against the real AI Gateway (requires `MAKERS_MODELS_KEY` to be available in the environment running this command — same env var `generate.js` reads via `env.MAKERS_MODELS_KEY`; for a local Node run this means it must be exported as a process environment variable, e.g. `MAKERS_MODELS_KEY=<value> npx promptfoo eval ...`, since `evals/provider.js` reads it via `process.env.MAKERS_MODELS_KEY`). Do not treat a less-than-100%-pass-rate result as a failure to fix in this task — CONTEXT.md D-05 explicitly scopes this phase's gate as "the eval suite exists and runs," not "100% pass rate"; a first-run baseline surfacing some failures is the expected and acceptable outcome.
  </action>
  <verify>
    <automated>test -f evals/results/latest.json && echo "baseline exists"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\"promptfoo\"" package.json` appears under a `devDependencies` key, not `dependencies`
    - `test -f evals/results/latest.json` succeeds (the baseline run produced output)
    - The eval run completed (exited, whether pass or fail) rather than hanging/timing out — confirm by inspecting the command's exit and `evals/results/latest.json`'s presence/non-empty content
    - No task in this plan reports a required 100% pass rate as a blocker — a partial-pass baseline is an acceptable, expected outcome per CONTEXT.md D-05
  </acceptance_criteria>
  <done>`promptfoo` is installed as a devDependency, and a baseline `npx promptfoo eval` run against the real AI Gateway has produced `evals/results/latest.json` — establishing the first-ever automated eval coverage for this phase's four dimensions, regardless of pass rate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> node_modules (dev dependency) | Supply-chain trust boundary — a malicious/slopsquatted package would execute arbitrary code during install or at `npx promptfoo eval` runtime |
| evals/provider.js -> AI Gateway | Same external HTTPS call generate.js already makes, now also made from a Node.js dev-tooling context outside the edge runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-05 | Tampering | `npm install --save-dev promptfoo` (flagged `[SUS]`, reason `too-new`) | high | mitigate | Blocking `checkpoint:human-verify` (Task 2) confirms repository/license/download-profile/no-postinstall-script before install; never auto-approved regardless of `workflow.auto_advance` |
| T-04.2-06 | Information Disclosure | `evals/provider.js` reading `MAKERS_MODELS_KEY` from `process.env` | low | accept | Same secret already used server-side by `generate.js`; the eval run is a manual, developer-invoked dev-tooling command, not a new production code path — the key is never written to `evals/results/latest.json` or any committed file |

</threat_model>

<verification>
- All six evals/ files exist and pass `node --check` where applicable
- schema-compliance.js imports validateWidget() directly, never redeclares enums
- The package-legitimacy checkpoint is explicitly approved before install
- A baseline evals/results/latest.json exists after Task 3
</verification>

<success_criteria>
- The evals/ directory is a complete, runnable Promptfoo suite per 04-AI-SPEC.md Section 5's design, authored without needing promptfoo installed
- promptfoo is installed only after explicit human confirmation of its package identity
- A first-run baseline eval report exists, establishing this phase's D-01 deliverable regardless of pass rate
</success_criteria>

<output>
## Artifacts this phase produces (Plan 04.2-03)

| Artifact | Type | Provides |
|----------|------|----------|
| `evals/promptfooconfig.yaml` | file (new) | Promptfoo run config wiring provider/prompt/testcases/assertions together |
| `evals/provider.js` (`GatewayProvider`) | file (new) | Node.js reimplementation of `generate.js`'s AI Gateway call, for use by `promptfoo eval` |
| `evals/system-prompt.txt` | file (new) | Mirrors `buildSystemPrompt()`'s vocabulary description for both data sources |
| `evals/testcases.yaml` | file (new) | 15-example reference dataset per `04-AI-SPEC.md` Section 5's composition |
| `evals/assertions/schema-compliance.js` | file (new) | Imports `validateWidget()` directly from `generation-schema.js` — no re-declared enums |
| `evals/assertions/interval-coherence.js` (`isCoherentPairing()`) | file + function (new) | Pure function checking `interval`/`timeRange` pairing coherence — no prior analog in this codebase |
| `evals/results/latest.json` | file (new, generated) | Baseline `npx promptfoo eval` run output — first-ever automated eval coverage for this pipeline |
| `package.json` `devDependencies.promptfoo` | config (modified) | The installed eval-running package, gated behind Task 2's blocking legitimacy checkpoint |

Create `.planning/phases/04.2-generation-pipeline-reliability-observability/04.2-03-SUMMARY.md` when done
</output>
